{
  "name": "cdk-template",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
      "build": "pnpm run -r build"
  },
  "devDependencies": {
    "typescript": "~4.9.3",
    "esbuild": "^0.24.0"
  }
}
